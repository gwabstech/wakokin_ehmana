<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gwabs.wakokinal_aminehmana">

    <application
        android:allowBackup="true"
        android:icon="@drawable/playerimg"
        android:label="@string/app_name"
        android:roundIcon="@id/imgmusicImage"
        android:supportsRtl="true"
        android:theme="@style/Theme.WAKOKINALAMINEHMANA">
        <activity android:name=".MainActivity" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>